import { Contact } from '../contact';
import { Projects } from '../projects';
import { Skills } from '../skills';

export const Main = () => {

  return (
    <main>
      <Skills />
      <Projects />
      <Contact />
    </main>
  )
}